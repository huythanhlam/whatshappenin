// Single source of truth for public contact details, reused across the footer
// and the legal/info pages (privacy, contact) so the handle and support address
// aren't re-typed — and can't drift — in a handful of separate files.

export const INSTAGRAM_HANDLE = '@whatshappenin.atx'
export const INSTAGRAM_URL = 'https://www.instagram.com/whatshappenin.atx/'

// Support inbox. The brand is "Whats Happenin" but mail is handled on the
// whatitdo.app domain (forwarding set up at the registrar — not in code).
export const SUPPORT_EMAIL = 'hello@whatitdo.app'
