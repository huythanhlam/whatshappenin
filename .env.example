# =============================================================================
# what-it-do — environment configuration
# Copy this file to `.env.local` and fill in the values you have. Every source
# degrades gracefully: a missing key just means that source contributes nothing
# (it never breaks the ingest run).
# =============================================================================

# --- Event source: Ticketmaster --------------------------------------------
# Structured Austin events (image-rich). Free key: https://developer.ticketmaster.com
# Without it, lib/scrapers/ticketmaster.ts returns [] and logs a warning.
TICKETMASTER_API_KEY=

# --- Event source: Eventbrite -----------------------------------------------
# NOTE: Eventbrite killed its public event-search API in Feb 2020, so NO token
# is needed anymore — lib/scrapers/eventbrite.ts scrapes Eventbrite's public
# Austin listing pages (schema.org JSON-LD). Nothing to configure here.

# --- LLM extraction: Google Gemini ------------------------------------------
# REQUIRED for newspapers, social, YouTube, and page-crawler sources. Those are
# prose feeds; each item is run through Gemini to pull out concrete, dated
# events. No key -> those four sources return []. Free tier: https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Budget/rate safety rails, enforced centrally by the one Gemini client.
# GEMINI_DAILY_BUDGET is a HARD daily request ceiling, enforced GLOBALLY in prod
# across all cron invocations (gemini_usage table) — so it's a real cost cap, not
# a per-process guess. GEMINI_RPM is the per-minute burst limit.
#   Free tier:  leave defaults (budget 500 is fine; the ~20/day model cap bites
#               first and 429s fall back to keyword tagging).
#   Paid tier:  RAISE GEMINI_RPM (e.g. 1000) — the free-tier 5/min will otherwise
#               serialize a heavy cron window past maxDuration and re-orphan it.
# GEMINI_DAILY_BUDGET=500   # max Gemini requests per UTC day (global, prod)
# GEMINI_RPM=5              # max Gemini requests per minute per invocation

# --- Event source: SeatGeek -------------------------------------------------
# Free client id: https://seatgeek.com/account/develop
SEATGEEK_CLIENT_ID=

# --- Event source: YouTube --------------------------------------------------
# YouTube Data API v3 key: https://console.cloud.google.com (needs GEMINI_API_KEY too)
YOUTUBE_API_KEY=

# --- Map view: Google Geocoding + Maps JS API --------------------------------
# Server-side key for venue geocoding (lib/geocode.ts), used once per unique
# venue at ingest time (cached in the `venues` table — never re-queried once
# cached). Enable the "Geocoding API" for it in https://console.cloud.google.com
# Without it, no venues get lat/lng and the map view simply shows no pins —
# nothing else breaks.
GOOGLE_GEOCODING_API_KEY=
# GEOCODE_DAILY_BUDGET=500   # max geocoding requests per day (ingest run)

# Client-side key for rendering the Maps JS API in the browser
# (components/MapView.tsx). NOTE: this key ships inside the public JS bundle —
# anyone can read it. In Google Cloud Console, restrict it with an HTTP
# referrer restriction (Credentials → your key → Application restrictions →
# HTTP referrers) scoped to your production domain(s); do NOT reuse the
# unrestricted GOOGLE_GEOCODING_API_KEY here — use a second, separate key with
# the "Maps JavaScript API" enabled. Without this key, the map view renders a
# "not configured" placeholder instead of a broken map.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# --- Optional: extend sources without code changes --------------------------
# Extra newspaper/blog RSS feeds. Format: url|source,url|source
# NEWSPAPER_FEEDS=https://example.com/feed|newspaper:example
# Extra pages to crawl each ingest (aggregators, link-in-bio pages):
# CRAWL_URLS=https://365thingsaustin.com,https://do512.com
# Headless-browser render service for JS-heavy/blocked pages (crawl4ai, Browserless, etc.):
# BROWSER_FETCH_URL=
# BROWSER_FETCH_TOKEN=

# --- Production: database + cron auth ---------------------------------------
# Local dev uses embedded PGlite and needs none of these. Set them for prod.
#
# DATABASE_URL selects the production Postgres. Use the Supabase Supavisor
# TRANSACTION pooler connection string, port 6543 (Dashboard → Project Settings
# → Database → Connection pooling → "Transaction" tab → URI) — NOT the Session
# pooler (port 5432): the app opens a `pg` Pool per serverless instance, and
# Session mode's low concurrent-client cap (its own `pool_size`, e.g. 15) gets
# exhausted by ordinary traffic, failing with "max clients reached in session
# mode". The one-off `npm run migrate` script also accepts either mode, since
# it only ever opens one connection.
# When DATABASE_URL is set, the app connects via this pool and PGlite is off;
# when unset, the app runs entirely on embedded PGlite (zero credentials).
# Apply migrations to it once with:  npm run migrate
DATABASE_URL=
# TLS certificate verification is ON by default for any non-local DATABASE_URL
# (Supavisor's cert is publicly CA-signed, so this just works). If you hit
# "self-signed certificate in certificate chain" / SELF_SIGNED_CERT_IN_CHAIN
# against a Supavisor pooler whose chain Node can't validate on its own, set
# DATABASE_CA_CERT below rather than reaching for DATABASE_SSL_INSECURE.
#
# DATABASE_CA_CERT — the PEM-encoded CA certificate content (not a file path
# — serverless has no persistent filesystem to reference one from). For
# Supabase: Dashboard → Project Settings → Database → SSL Configuration →
# download the CA certificate, then paste its full contents here (Vercel's
# env var input accepts multi-line values). When set, the app verifies the
# connection against this specific CA instead of the system trust store —
# keeping certificate verification on. Takes precedence over
# DATABASE_SSL_INSECURE below.
# DATABASE_CA_CERT=
#
# DATABASE_SSL_INSECURE — last resort only, for a genuinely self-signed
# deployment with no CA cert to pin. Disables certificate verification
# entirely for the connection, which allows a network-level attacker to
# impersonate the DB. Prefer DATABASE_CA_CERT above whenever a CA cert is
# obtainable.
# DATABASE_SSL_INSECURE=true
# Bearer token that guards the mutation/cron routes (/api/ingest, /api/import,
# /api/featured, /api/email/digest). REQUIRED in production — without it those
# routes refuse to run (503) instead of running open. Vercel Cron sends it
# automatically once set. Generate: openssl rand -hex 32
CRON_SECRET=

# Canonical public origin, e.g. https://whatitdo.app — used for unsubscribe
# links in emails AND for every SEO surface: page canonicals (metadataBase),
# OpenGraph URLs, robots.txt host, and the per-city sitemaps. MUST be set in
# production to the real apex domain (no trailing slash, no www) or those URLs
# fall back to the per-deployment VERCEL_URL and search engines index a preview
# host instead of your domain.
SITE_URL=

# --- SEO: search-engine site verification -----------------------------------
# Ownership tokens for Google Search Console and Bing Webmaster Tools. Paste the
# HTML-tag verification value (just the token, not the whole <meta> tag) and the
# app emits the corresponding <meta name="..."> in <head>. DNS-TXT verification
# is an alternative that needs neither of these. Leave blank to emit nothing.
GOOGLE_SITE_VERIFICATION=
BING_SITE_VERIFICATION=

# --- Optional: email digests ------------------------------------------------
# RESEND_API_KEY=
# Verified sender for digest/confirmation emails, e.g. "What It Do Austin
# <hello@yourdomain.com>". Without it, Resend's sandbox sender is used, which
# only delivers to the Resend account owner.
# EMAIL_FROM=

# --- Supabase Auth + RLS (re-architecture) ----------------------------------
# User identity is Supabase Auth (GoTrue) and user-private data is read/written
# through PostgREST under the end user's JWT, so RLS enforces per-user access.
# Locally, run the Supabase CLI stack (`supabase start`) and paste the values it
# prints; in prod, use your project's values from the Supabase dashboard.
#
# Public (safe to expose to the browser):
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only, bypasses RLS — NEVER expose to the client. Used for account
# deletion and confirmed-subscription creation at sign-up.
SUPABASE_SERVICE_ROLE_KEY=

# --- Google sign-in ("Continue with Google") --------------------------------
# OAuth via Supabase Auth. Two places to configure, depending on environment:
#
# 1. Google Cloud Console (https://console.cloud.google.com → APIs & Services →
#    Credentials): create an "OAuth client ID" of type "Web application".
#    - Configure the OAuth consent screen with scopes: openid,
#      .../auth/userinfo.email, .../auth/userinfo.profile.
#    - Authorized redirect URI = your Supabase project's callback:
#         https://<project-ref>.supabase.co/auth/v1/callback   (prod)
#         http://localhost:54321/auth/v1/callback              (local CLI stack)
#    Copy the generated Client ID and Client secret.
#
# 2a. LOCAL dev: paste the two values below. supabase/config.toml reads them via
#     env() when you run `supabase start`. (PGlite dev without the CLI stack
#     can't exercise real Google OAuth — use the hosted project for that.)
# 2b. PRODUCTION: do NOT rely on these vars. Enter the same Client ID/secret in
#     the Supabase dashboard (Authentication → Providers → Google) instead, and
#     add your app origin (e.g. https://whatitdo.app) to the provider's allowed
#     redirect URLs so /auth/callback is accepted.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=
